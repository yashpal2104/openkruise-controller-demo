---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: miniclonesets.apps.example.com.my.domain
spec:
  group: apps.example.com.my.domain
  names:
    kind: MiniCloneSet
    listKind: MiniCloneSetList
    plural: miniclonesets
    singular: minicloneset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MiniCloneSet is the Schema for the miniclonesets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MiniCloneSet
            properties:
              image:
                description: Image specifies the container image to use
                minLength: 1
                type: string
              replicas:
                default: 1
                description: Replicas specifies the number of desired replicas
                minimum: 0
                type: integer
              updateStrategy:
                default: RollingUpdate
                description: UpdateStrategy specifies the strategy to use when updating
                  pods
                enum:
                - RollingUpdate
                - Recreate
                type: string
            required:
            - image
            - replicas
            type: object
          status:
            description: status defines the observed state of MiniCloneSet
            properties:
              availableReplicas:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: integer
            required:
            - availableReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: MiniCloneSet is the Schema for the miniclonesets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MiniCloneSet
            properties:
              container:
                description: Container specifies the container configuration
                properties:
                  image:
                    description: Image specifies the container image to use
                    minLength: 1
                    type: string
                required:
                - image
                type: object
              replicas:
                default: 1
                description: Replicas specifies the number of desired replicas
                minimum: 0
                type: integer
              updateStrategy:
                description: UpdateStrategy specifies the strategy to use when updating
                  pods
                properties:
                  maxUnavailable:
                    default: 25%
                    description: MaxUnavailable specifies the max number of unavailable
                      pods during update
                    type: string
                  type:
                    default: RollingUpdate
                    description: Type specifies the update strategy type
                    enum:
                    - RollingUpdate
                    - Recreate
                    type: string
                type: object
            required:
            - container
            - replicas
            type: object
          status:
            description: status defines the observed state of MiniCloneSet
            properties:
              availableReplicas:
                description: AvailableReplicas indicates the number of available replicas
                type: integer
            required:
            - availableReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
